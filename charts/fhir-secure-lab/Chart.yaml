apiVersion: v2
name: fhir-secure-lab
description: HAPI FHIR + PostgreSQL + Keycloak + Envoy (JWT validation) on Minikube
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: oauth2-proxy
    version: 0.1.0
    repository: "file://charts/oauth2-proxy"
    condition: oauth2Proxy.enabled
